# The marketing site currently reads no environment variables — every page is
# static. This file is a placeholder for when that changes.
#
# Copy to .env.local for local dev, and add the same keys in
# Vercel → Project → Settings → Environment Variables.

# Example: wiring up /api/contact so form submissions are actually stored.
# See "Known gaps" in README.md — right now that route validates and discards.
# CONTACT_FORM_WEBHOOK_URL=
