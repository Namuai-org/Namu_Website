export default function StudioDashboardPage() {
  return (
    <main style={{ minHeight: "100vh", padding: "48px 24px", fontFamily: "var(--font-body)" }}>
      <h1 style={{ fontFamily: "var(--font-display)", fontSize: "42px", marginBottom: "16px" }}>Studio Dashboard</h1>
      <p>Authenticated users land here from the landing CTAs.</p>
    </main>
  );
}
